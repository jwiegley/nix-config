name: web-local-extract
version: 1.0.0
description: "Direct URL→Markdown extraction via trafilatura. No API key or intermediary extraction service; the requested origin still receives the normal HTTP request."
author: vulcan
kind: backend
provides_web_providers:
  - local
