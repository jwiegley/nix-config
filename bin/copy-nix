#!/bin/bash

HOST=$1
shift 1

nix copy --to $HOST                             \
    $(readlink -f ~/.nix-profile)               \
    $(readlink -f /run/current-system)

push -f Projects,Contracts,home $HOST

ssh $HOST darwin-rebuild switch
ssh $HOST home-manager switch
