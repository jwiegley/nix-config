#!/usr/bin/env bash
find -L \
    ~/src/ \
    ~/Models/ \
    ~/doc/ \
    ~/Databases/ \
    ~/work/ \
    -name .git -type d \
    \! -path '*/gists/*' \
    -print0 |
    parallel -0 -j4 "update {//}"
