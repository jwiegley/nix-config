Use $ARGUMENTS to analyze and resolve the merge conflicts in the current working tree in a way that preserves the semantics of the incoming changes, while maintaining the intent of the current working tree's work. Do not commit your work when you're done, just leave the resolved files in the working tree after you use `git add` to mark them resolved.
