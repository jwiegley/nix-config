Add a `lefthook.yml` for $ARGUMENTS with pre-commit checks for code formatting, building with no warnings, running tests, linting, and code-coverage checking.

This is the standalone pre-commit-hooks slice of the `productize` workflow: follow that workflow's lefthook / pre-commit section (including its per-language hook setup) for the details, without performing the rest of productization.
