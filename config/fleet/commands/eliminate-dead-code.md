Find and remove dead code and stale documentation for the scope in `$ARGUMENTS` (a path, `docs`, `imports`, `feature-flags`, `comments`, a language name, `cap=N`, `recent=Nd`, or empty for the full repository).

Follow the `eliminate-dead-code` skill for the full methodology: the mark / debate / act / verify workflow, the operating principles (conservative-by-default, two-evidence rule for dynamic languages, native tooling first, blast-radius cap, markers never escape), the per-phase detail in its references (discovery, the exclusion allowlist, static-analysis tooling, cross-reference verification, the marker format and sidecar schema, the three-advocate debate, the act loop, and the verify checklist), and the report template and approval gates.

$ARGUMENTS
