diff --git a/dist/modes/interactive/components/model-selector.js b/dist/modes/interactive/components/model-selector.js
index de1ebc1..95db21d 100644
--- a/dist/modes/interactive/components/model-selector.js
+++ b/dist/modes/interactive/components/model-selector.js
@@ -117,18 +117,24 @@ export class ModelSelectorComponent extends Container {
             currentIndex >= 0 ? currentIndex : Math.min(this.selectedIndex, Math.max(0, this.filteredModels.length - 1));
     }
     async refreshModels() {
-        const timeoutMs = 15_000;
+        const timeoutMs = 2_500;
         let timedOut = false;
-        this.refreshTimeout = setTimeout(() => {
-            timedOut = true;
-            this.refreshAbortController.abort();
-        }, timeoutMs);
+        const timeoutResult = new Promise((resolve) => {
+            this.refreshTimeout = setTimeout(() => {
+                timedOut = true;
+                this.refreshAbortController.abort();
+                resolve(undefined);
+            }, timeoutMs);
+        });
         try {
-            const result = await refreshModelCatalogs(this.modelRuntime, this.refreshAbortController.signal);
+            const result = await Promise.race([
+                refreshModelCatalogs(this.modelRuntime, this.refreshAbortController.signal),
+                timeoutResult,
+            ]);
             if (this.closed)
                 return;
             this.refreshStatusMessage = "";
-            if (result.aborted && timedOut) {
+            if (timedOut) {
                 this.errorMessage = "Model refresh timed out; showing cached models.";
             }
             else if (result.errors.size === 1) {
diff --git a/dist/modes/interactive/interactive-mode.js b/dist/modes/interactive/interactive-mode.js
index 771f922..4906d42 100644
--- a/dist/modes/interactive/interactive-mode.js
+++ b/dist/modes/interactive/interactive-mode.js
@@ -778,14 +778,6 @@ export class InteractiveMode {
      */
     async run() {
         await this.init();
-        if (!process.env.PI_OFFLINE) {
-            const controller = new AbortController();
-            const timeout = setTimeout(() => controller.abort(), 15_000);
-            void refreshModelCatalogs(this.session.modelRuntime, controller.signal)
-                .then(() => this.updateAvailableProviderCount())
-                .catch(() => { })
-                .finally(() => clearTimeout(timeout));
-        }
         // Start version check asynchronously
         checkForNewPiVersion(this.version).then((newRelease) => {
             if (newRelease) {
