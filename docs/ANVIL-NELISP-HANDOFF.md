# Anvil Fleet Backend Handoff

Updated: 2026-07-15

## Current position

The current production source is the Anvil fork commit `74c33b38240809a6d37cbcac957ab04fb1525b78`, fetched with NAR hash `sha256-7K+p2gwat8c/A54CnIRMrrv1hpm6zpDwkkbebYqHZ3I=`. The commit is pushed on `fix/issue-53-staging-cleanup` in `jwiegley/anvil.el`; it is intentionally unsigned because no non-interactive signing credential was available. Nix deployment commit `f842b28922ea0bf73561f8bf3d59b18b38c0b961` pins that exact source and is pushed to `main` on both Nix remotes.

Every dedicated fleet target runs this generation. Hera is Darwin generation 861 at `/nix/store/vc51vplj3k4rfi79vinxq265m5na29c2-darwin-system-26.11.d5bd9cd`. Clio is Darwin generation 222 at `/nix/store/dyg84kw26690s3iqd2879i6194233xjr-darwin-system-26.11.d5bd9cd`. Vulcan is NixOS generation 2210 at `/nix/store/6ynjnz916vsgp8gld5x33dj96hywrm69-nixos-system-vulcan-25.11.20260630.b6018f8`; consumer commit `6a4815192c29366bb48607b3a5fe0f35f2a5fd10` pins `nix-config` to `f842b289` and the required promptdeploy companion to `c308988`. The shared work fleet uses Home Manager generation 162 at `/nix/store/g42q7611yray43apkxcnqknfk7i2fbbk-home-manager-generation` and profile generation 158 at `/nix/store/bllijl7px0jpd18mw3cmvrrl5g6fc1b9-user-environment`; pushed consumer commits `c6ee304` and `f1cecc3` provide the same two exact inputs.

Fresh post-switch clients on Hera, Clio, and Vulcan independently exposed 89 unified and 76 direct typed tools, returned 42 from real Emacs evaluation, preserved raw bytes when `filter=""`, and left no test-owned process or filesystem residue. Host-specific recovery proofs covered isolated failure recovery on Hera and Vulcan and four-worker reset with root continuity on Clio. Clio and Vulcan additionally accepted real 2 MiB MCP requests; Vulcan also gave two simultaneous clients distinct root PIDs. All four work hosts passed the same 89/76, eval, passthrough, four-worker reset/recovery, and exact-cleanup checks; Andoria-08 additionally accepted a 2,097,163-byte expression carrying a 2 MiB payload. Earlier diagnosis temporarily added a dot-emacs compatibility shim, but the upstream repair made it obsolete and dot-emacs commit `09cbe743` removes it; no additional dot-emacs Lisp change was needed for this final transport/lifecycle round. The final defects were in Anvil's stdio/readiness/custody behavior and in the Nix lifecycle, packaging, and verification code.

The Linux contract is `johnw.anvil.useHeadlessEmacs`: false selects the proven NeLisp v0.5.1/anvil.el v1.1.1 pair; true selects current anvil.el 1.3.0 plus anvil-ide under `emacs30-nox`. The independent Darwin contract is `johnw.anvil.useDedicatedDarwinEmacs`: false uses the current interactive Emacs; true reuses the pinned MacPort Emacs in a separate minimal daemon. The PATH launcher remains `anvil-mcp`. Dedicated modes load the full configured optional set, retain 76 direct typed tools, and mirror them into the 13-tool main registry, yielding one 89-tool main surface without promptdeploy knowing either Boolean.

## 2026-07-15 readiness and transport-custody hardening

This section supersedes conflicting claims in the dated 2026-07-14-and-earlier history below; the later current-contract and verification sections incorporate this refresh.

- The final upstream work closes readiness false positives, staged-request ownership gaps, partial marker races, bounded frame reads, pipe recovery, cleanup during signal transitions, and descendant custody. The complete aggregate suite passed 107 files: 2,469 executed and passed, 235 intentionally skipped, and zero failed out of 2,704 discovered tests. Both the macOS Bash 3.2 and modern Bash 5.3 stdio harnesses passed.
- The final Nix checks passed Python compilation and Ruff, nixfmt, scoped statix and deadnix, `git diff --check`, `nix flake check --no-build --all-systems`, the dedicated Darwin package check, and native dedicated smoke derivations on x86_64 Linux and aarch64 Linux. Both Linux architectures reported two isolated daemons, 89 unified tools, and 76 typed tools.
- The definitive final-derivation soak passed 25/25 recovery cycles with async isolation, direnv, pipelined file/Org/Git/Elisp work, and exact cleanup. Dispatch was 45.504–46.076 seconds, readiness 2.252–10.998 seconds, restart 0.801–1.347 seconds, and healthy-sibling work 1.955–2.930 seconds; first-versus-last medians showed no degradation. No temporary, process, or staged-request residue remained.
- The soak's named internal phases total 8,001 seconds. A required 20% margin gives a 9,602-second TERM deadline, the 90-second kill grace gives a 9,692-second absolute KILL horizon, and the asserted policy ceiling is 9,720 seconds. Setup, inventory, post-recovery child isolation, partial-marker polling, remaining-deadline caps, and signal-safe cleanup all have explicit independent bounds.
- The shared work profile initially propagated through NFS before its new host-local closures were all present. The acceptance sweep caught the broken launcher links before starting Anvil, restored the exact immutable closure, and verified all 1,931 references on each host. Andoria-08, gpu-server, Andoria-t2, and Delphi-3bd4 then each passed a fresh isolated client; pool reset replaced all four worker PIDs while preserving the root PID, post-reset evaluation remained healthy, EOF exited cleanly, and the final test-owned process/socket/root audit was empty. No repository, profile link, or pre-existing client was altered.
- Clio's final deployment used a clean detached worktree at `e7aef657` while preserving its shared checkout's sole `flake.lock` modification byte-for-byte (`sha256:1a1d721a21d840c26258502a09e8102a44ffd0aa076246de6ed7535c250c26d3`). The exact build and separate locked switch both passed. A fresh installed client exposed 89/76 tools, returned 42 before and after replacing all four worker PIDs while preserving root PID 93861, passed exact empty-filter passthrough and a 2,097,273-byte wire request carrying a 2 MiB payload, exited cleanly on EOF, and left no test-owned process, file, socket, or root residue.
- Dedicated/headless Anvil is the canonical production service. Interactive main Emacs may use the same corrected source, but it is intentionally a separate, non-equivalent failure domain and is not certified by these dedicated-service results. The guardian contains same-process-group descendants; it cannot honestly promise custody of a deliberately detached `setsid` escape. These are defined support boundaries, not open implementation defects, and no finite test campaign can make a service literally failure-proof.
- No post-OS-reboot soak was performed in this refresh. Identity-safe cleanup deliberately fails closed on a pathological nested mode-`000` directory because the supported platforms do not provide the portable no-follow descriptor bootstrap needed to widen it without a race; ordinary private trees, read-only nested caches, staged requests, sockets, and owned descendants are covered by the passing cleanup tests.

## 2026-07-14 large-stdio hardening refresh

This later refresh supersedes the source pins, package generations, store paths, and deployment revisions recorded in the earlier 2026-07-14 closure below.

- Stress testing exposed three additional transport defects: quadratic metadata parsing, `ARG_MAX` failure from passing large JSON-RPC bodies through `emacsclient`, and frame over-read that could discard pipelined bytes. Signed upstream commit `8bdcceeccfaf5351b1faf43f96adfc1fbcdb05c4` stages bodies larger than 16 KiB in private files, removes them before dispatch, reads exact bounded frames, and covers Unicode byte lengths, hostile temporary roots, pipelining, permissions, and same-pipe recovery.
- Signed Nix commit `eef6693071f92b41bb2bc9456166c4b28e2582b4` pins that exact upstream revision, keeps staged requests inside each supervised private runtime, configures the packaged NeLisp source directory, and adds 512 KiB Unicode request and cleanup regressions through both host and per-agent production paths. No dot-emacs Lisp change was required for this defect; the necessary changes were in `anvil.el` and the Nix packaging and verification code.
- The complete upstream suite passed 105 files, 2,618 discovered tests, 2,383 executions, 235 capability-gated skips, and zero failures. Real 512 KiB and 16 MiB requests, repeated 16 MiB requests, Unicode framing, pipelining, and recovery behind the same pipe also passed.
- The complete Nix flake gate passed, including the 89-tool unified surface, 76 typed tools, NeLisp evaluation, hostile ambient temporary state, exact cleanup, and the 25-cycle persistent recovery soak. Across that soak, forced dispatch took 45.655-53.243 seconds, healthy siblings 3.843-19.900 seconds, restart 0.752-1.436 seconds, and readiness 3.625-43.760 seconds, without replay, leakage, or latency growth.
- Hera runs Darwin generation 859 at `/nix/store/iiczn8hgs37j81s0sj9iqdk5rlilxbcn-darwin-system-26.11.d5bd9cd`; Clio runs generation 218 at `/nix/store/nja28knck0l557c8kxwmr0ccask4h3ph-darwin-system-26.11.d5bd9cd`. Both use launcher `/nix/store/jvdd4d9sr3pckz2w4k0xfshz974zxk6k-anvil-mcp/bin/anvil-mcp`, supervisor `/nix/store/my1i2598mgjp9slxpn41yn69n1lk524z-anvil-agent-supervisor.py`, and package generation `d4df96eac7891d36116ba6b1360aa3a2493827ce4e6e54283f1fb2d7658370b9`. Fresh installed processes on both hosts exposed 89 unified and 76 typed tools, returned 42 from both Emacs and NeLisp evaluation, accepted the 524,289-character Unicode request, and left no staged files or isolated roots.
- Vulcan runs NixOS generation 2209 at `/nix/store/lzbik6km74zga4ixr0j6m1rrbyhhkff9-nixos-system-vulcan-25.11.20260630.b6018f8`. Its installed launcher `/nix/store/wy8j7vix4h0n8wz4b9zclag8045i0zh4-anvil-mcp/bin/anvil-mcp` embeds supervisor `/nix/store/xlg46gcqnpyqbhnlk468689v92hjdfvw-anvil-agent-supervisor.py`, stdio source `/nix/store/1ga53ql4w57ljddx6g124y35wwf668ab-anvil/share/emacs/site-lisp/anvil-stdio.sh`, and generation `ebe3bd56ada52a1433e5d3c015290ecae150dc40461ce5f52d89cf7ec0fff0f0`. Native ARM64 package coverage and the same fresh installed 89/76, Emacs/NeLisp, 524,289-character, and empty-cleanup proof passed. Consumer lock commit `3b16f9e93e152ede5d2933dcc8eefa3974a56c9a` is pushed.
- The shared work-host Home Manager checkout is pushed at `652d370a45bb1921b5efef594179531a6c985bbd` and locks `nix-config` to `eef6693`. Before the shared profile changed, activation closure `/nix/store/kdjfwcfqw7hixcykm4hhdpx5q6als5hp-home-manager-generation` was realized independently in the host-local Nix stores on Andoria-08, Andoria-t2, Delphi-3bd4, and gpu-server. All four now use launcher `/nix/store/48ahdlq0cmk7zis20fzyn0nml8dq6vyx-anvil-mcp/bin/anvil-mcp`, supervisor `/nix/store/v1b1h4807970q7byipf3hpna1d2lmgqg-anvil-agent-supervisor.py`, stdio source `/nix/store/0g2j97qmrkdwd3vvpfsyr8vwydzwzp08-anvil/share/emacs/site-lisp/anvil-stdio.sh`, and generation `6a51e49573eae00af086187e694e3d69b1aff7e78f150bb0a19c55d16ac5bdbf`. The native x86_64 package gate passed, and fresh installed processes on every host independently passed the same 89/76, Emacs/NeLisp, 524,289-character, and empty-cleanup proof.
- Dedicated headless Emacs remains the production recommendation because it isolates editor state and failure domains. Interactive main Emacs uses the same corrected transport source but does not provide that isolation. The supported and certified fleet remains macOS and Linux; Windows large-request behavior is not certified, and no service can honestly be described as failure-proof.

## 2026-07-14 cleanup and fleet closure

- The final live defect was stale-tree removal beneath a dead private runtime root when a nested cache directory was mode `0555`. `remove_directory_contents` now validates the child's pre-open identity, opened descriptor, post-open identity, type, and owner before widening only that open descriptor to mode `0700` and recursing. A replacement-race regression proves that a path swapped before open is rejected without changing or deleting either tree; a read-only nested-cache regression proves cleanup while preserving an outside symlink target. A pathological nested mode-`000` directory still fails closed and remains unpruned because the supported platforms do not share a portable race-safe, no-follow descriptor bootstrap for it. The change is confined to the Nix-packaged supervisor and its tests; no change was needed in anvil.el or dot-emacs Lisp.
- The five queued blocker groups captured in `questions.png` are closed in the pinned source and package: dead-process finalization snapshots every matching registration and nils callbacks before invocation; retired-pool cleanup reconstructs surviving generations from immutable local vectors after callbacks; the Nix-owned hang fixture uses the canonical fail-closed reset and runs the installed ownership suite; Darwin interactive and dedicated packages share one source record; and malformed or unreadable wire values terminally fail and retire their exact owner. The focused ownership file passes 44/44, the same-process isolation-plus-ownership run passes 76/76 including the hidden-state assertion, the hang regressions pass 7/7, and the opt-in offload suite passes 24/24.
- The focused supervisor suite passes all 87 tests; Ruff lint and formatting pass; the dedicated native smoke passes with two daemons, 89 unified tools, 76 typed tools, restart and lock recovery; the 25-cycle Hera soak passes; `nix flake check -L --keep-going`, `./build system`, and the Darwin switch all pass. Commit `9d79fef80198c727d95c6dfac7ea551151674a4b` has a good EDDSA signature and is pushed on `main` to both Nix remotes.
- Hera runs Darwin generation 857 at `/nix/store/xbcwiym1vdnyrlrphas0b0l7gq8ny9vx-darwin-system-26.11.d5bd9cd`, launcher `/nix/store/35qczycc3mhz5s9f18n78sfbmkjcnyif-anvil-mcp/bin/anvil-mcp`, supervisor `/nix/store/lk6bzp6wvpzps3wgdvglyxp42qk2kb6y-anvil-agent-supervisor.py`, and package generation `5f88e988a4babe728e602b442ea0f598532b0464ec20a958f400640f6aabfa25`. A fresh installed dual-client probe passed real eval and file work, isolated a 45.745-second non-yielding failure while its sibling answered in 0.157 seconds, restarted the failed root, and left no temporary roots. A post-switch audit found runtime and state identities paired, private, and owned by live client trees; the known read-only orphan was pruned by the production code.
- Clio runs Darwin generation 216 at `/nix/store/ccr7ffpzjflvgj95m8jp6nh7n7hmhhc6-darwin-system-26.11.d5bd9cd`, with the same launcher, supervisor, and package generation as Hera. A fresh installed dual-client probe exposed 89 unified and 76 typed tools, passed real eval and file work, isolated a 45.735-second non-yielding failure while its sibling answered in 0.353 seconds, restarted the failed root, and left no temporary roots. The configured Claude server reports connected and Codex renders 210/210-second timeouts. The dead legacy shared tree was removed only after process, descriptor, socket, and identity checks. The old GUI Emacs later exited without agent intervention; its modified journal's mtime advanced at exit and no autosave remained. The replacement GUI Emacs has no modified file buffers, does not load Anvil, and is outside the dedicated service's failure domain.
- Vulcan runs NixOS system `/nix/store/jsg6dp6hb9rzshcqaf6g32y1fmw0f39h-nixos-system-vulcan-25.11.20260630.b6018f8`, launcher `/nix/store/y4f2dfa4a959jnk0kn835rrdafkhcyjd-anvil-mcp/bin/anvil-mcp`, supervisor `/nix/store/pkrsivnq78609ksnh7gbsvgzq9agawjk-anvil-agent-supervisor.py`, and package generation `68bb8f74045bf57dbafdacb44cc68b508c2862a2e32cad8346be2fa8a3bb810c`. A fresh installed dual-client probe exposed 89/76 tools, passed real eval and file work, isolated a 45.556-second non-yielding failure while its sibling answered in 0.122 seconds, restarted the failed root, and left no temporary roots. Claude reports the configured server connected, Codex renders 210/210-second timeouts, and the NixOS lock/activation commit `c0a5a2a8f25a881fab4b4431b5248860341a6055` is pushed to Vulcan's origin. The native ARM64 persistent gate then passed all 25 cycles: dispatch 45.115–45.333 seconds, healthy sibling 0.375–0.445 seconds, restart 0.601–0.805 seconds, and readiness 1.187–1.478 seconds. Its final quiet-state check found no build lock, process tree, isolated root, or installed-service residue.
- The Andoria Home Manager checkout is clean and pushed at `6296268358cab3f0c0f3e629b88ba4dfc5c178fc`, with `nix-config` locked to exact `9d79fef`. That commit is unsigned because the work host has no private signing key; no key was copied there. Native x86_64 staging passed the same 87-test and 89/76-tool smoke plus a 25-cycle soak. All four hosts now resolve launcher `/nix/store/yrd2shcf01awnl5kmp49ghm5f2j3ymb9-anvil-mcp/bin/anvil-mcp`, supervisor `/nix/store/an7bq4nfh3ck64v9q7xg3hm4qbn684cy-anvil-agent-supervisor.py`, and generation `8a3817f3415b6f1abc67e8733c2f0d7870669c45fb82a6a7c435660dd2ec6154`.

| Work host | Forced failure | Healthy sibling | Result |
|---|---:|---:|---|
| Andoria-08 | 45.526s | 0.117s | 89/76 tools, eval/file, restart, empty cleanup |
| Andoria-t2 | 45.113s | 0.084s | 89/76 tools, eval/file, restart, empty cleanup |
| Delphi-3bd4 | 45.505s | 0.152s | 89/76 tools, eval/file, restart, empty cleanup |
| gpu-server | 45.801s | 0.417s | 89/76 tools, eval/file, restart, empty cleanup |

- The final work-host audit found mode-`0700` runtime and state roots, zero instance directories, and zero live Anvil processes on every host after transport shutdown. The native Andoria soak measured 45.210–45.333-second forced dispatches, 0.679–0.736-second sibling work, 0.601–0.803-second restarts, and 1.714–1.917-second readiness.
- Promptdeploy `4192b7e` passed its complete detached-worktree flake check: 1,787 tests with 100% coverage, mypy, Ruff, package build, and Home Manager activation checks. Exact local Anvil item verification also passes. The user's existing `commands/wiggum.md` modification remains untouched.
- At that earlier closure, the Anvil fork was clean at `5a062f7`, 19 commits ahead of and zero behind upstream `574568a`. VPS remained intentionally unchanged because `9d79fef` affected only the dedicated-Emacs supervisor, while VPS used the separately live-proved 42-tool standalone NeLisp backend.
- Evidence still outside this closure is explicit: no post-OS-restart soak was performed, and Claude's weekly quota prevented a model-level tool call even though its MCP status reports connected. A deliberately pathological nested mode-`000` stale directory remains a documented fail-closed cleanup limit. An absolute claim of being failure-proof is therefore impossible; within the supported threat and failure model, every deployed dedicated service path has passed its package, recovery, live-client, ownership, cleanup, and architecture-specific gates. Interactive main Emacs is intentionally not the production backend: keeping Anvil in dedicated headless Emacs is what prevents a tool hang from freezing the editor.

## 2026-07-13 production findings

This is a historical incident record of the then-deployed generation. The 2026-07-15 source and deployment above replace every topology, timeout, and client-state defect described here.

- The two deployed MCP registrations (`anvil` and `emacs-eval`) share one root per owning top-level Codex process. Internal agents therefore inherit the same root failure domain instead of receiving independent workers.
- `emacs-eval-async` schedules arbitrary evaluation back into the root with `run-with-timer`; it is asynchronous only from the caller's perspective and can freeze the root event loop.
- Synchronous dispatch and async work share one watchdog lease. Any active lease selects the 600-second deadline, so a synchronous root hang can outlive the bridge's 330-second transport budget.
- A shell tool that invokes Anvil or `emacsclient` against its own root can deadlock deterministically: the root waits with `accept-process-output` restricted to its child while the child waits for that root's server loop. The issue-53 fix correctly avoids servicing unrelated process filters, so recursion must be rejected rather than reintroducing reentrant callbacks.
- Supervisor keys and status do not include the packaged generation. A new launcher under a long-lived client can attach to an older responsive daemon, and already-executed stdio bridges cannot hot-upgrade. Full Codex and Claude OS-process restarts are required after activation.
- Claude has not received the new launcher configuration: live Personal and Positron registrations still reference the old interactive socket. Promptdeploy source is correct in part, but it was never activated and its manifest-only drift check can falsely report a manually stale MCP entry as current.
- In the then-deployed generation, supervisor stdout/stderr went to `/dev/null`, leaving insufficient bounded diagnostics when readiness or a daemon restart failed.
- A direct recursive reproduction held the then-deployed dedicated root at high CPU for more than two minutes before recovery. This failed acceptance test motivated the repair; it was not a theoretical risk.
- A live Codex source/process audit proved that every internal agent thread owns a distinct MCP stdio process even when all bridges have one top-level Codex parent. The deployed generation incorrectly coalesces those transports by the parent PID; the current bridge-PID/start-identity/package-generation key gives each transport its own daemon without any Codex patch. Codex also supplies `_meta.threadId`, but per-transport identity covers initialization and the complete protocol without adding a routing proxy.
- The first integrated request-10 failure sampled an auxiliary shell process stalled before user code in macOS `_dyld_start`. Replacing Bash-to-Python with a direct isolated-Python trampoline removes one cold loader launch and preserves descendant cleanup. A second request-10 failure showed that an artificial six-second host-smoke dispatch deadline still converts ordinary host-wide loader scheduling into a false root hang. The ordinary host transcript now uses the packaged production deadlines; the separate supervisor smoke continues to inject a real non-yielding form and proves same-pipe replacement, sibling isolation, and at-most-once behavior under accelerated deadlines.

## Timeout ordering contract

The cross-client timeout ladder preserves the full 120-second cooperative synchronous evaluation window while ensuring that each outer layer expires later than the complete serial path it supervises. Root heartbeat progress is due every 45 seconds; the hard root dispatch watchdog fires at 225 seconds; and ordinary stdio dispatch is capped at 240 seconds. JSON-RPC metadata parsing and bounded frame reading each have ten-second caps, each bounded emacsclient runner escalates from TERM to KILL after one second, bridge readiness has a 20-second budget, and each parent-guard handshake has a five-second budget. The full 288-second worst-case ordinary request envelope remains below the 330-second MCP client limit. Supervisor daemon shutdown separately uses bounded five-second TERM and post-KILL waits. Startup retains a separate 20-second initialize-dispatch cap; its complete cleaner, supervisor, parse/readiness, parent-guard, and kill-escalation envelope is 218 seconds and also fits beneath the client limit. Runtime overrides may shorten but cannot enlarge these maxima. Synchronous `shell-run` and `shell-tee-grep` overrides are capped at 120 seconds and rejected before spawning when larger; longer builds use native execution or the external asynchronous path. External asynchronous evaluation retains its independent 600-second default and does not lengthen the synchronous root watchdog.

The bridge no longer overloads one timeout variable for every phase: `ANVIL_MCP_REQUEST_PARSE_TIMEOUT` bounds metadata parsing, `ANVIL_EMACSCLIENT_PROBE_TIMEOUT` bounds each pure probe, `ANVIL_EMACSCLIENT_READINESS_TIMEOUT` bounds replayable readiness, `ANVIL_EMACSCLIENT_STARTUP_DISPATCH_TIMEOUT` bounds initialize, and `ANVIL_EMACSCLIENT_DISPATCH_TIMEOUT` bounds ordinary non-replayable dispatch. The generated Nix artifacts and a dedicated regression file are checked against one package-level timeout policy; promptdeploy's target-render tests independently bind startup and tool calls to the same 330/330-second client limits.

Home Manager exports `MCP_TIMEOUT=330000` for Claude startup, but only newly started processes that inherit the Home Manager session environment receive it. Existing shells and complete Codex/Claude OS processes must be restarted after activation. GUI-launched Darwin clients may not inherit shell session variables at all, so the rendered per-server client timeout remains authoritative rather than relying on this environment fallback.

## Evidence gathered

- Nix worktree: `/Users/johnw/src/nix`, branch `main`, initial HEAD `c988ea6dc269312af9d4545fc5d0b76f56cd801e`, initially clean.
- promptdeploy worktree: `/Users/johnw/src/promptdeploy`, branch `main`, initial HEAD `a037d18f4e40baaead3dbda4a41e956c097faadb`.
- At the initial audit, macOS Anvil was fetched and compiled in `overlays/10-emacs.nix`, included by `config/emacs.nix`, loaded in the running development Emacs, and served through its live socket.
- The packaged interactive Darwin launcher reached that initial daemon: `anvil` returned protocol 2025-03-26 and 13 tools; `emacs-eval` returned 65 tools. The user's report that agent traffic could intermittently lock the development Emacs motivated the now-active dedicated Darwin process.
- The dedicated Darwin check starts its MCP client before one daemon is ready, then proves bounded launcher waiting, two concurrent MacPort Emacs daemons, exactly 89 unified and 76 typed tools, real eval/file/Org/semantic calls, distinct socket/schema/state paths, private nested worker directories, isolated worker process environments, and clean shutdown. In host mode the evaluated launchd agent sets exactly `ANVIL_EMACS_LOCK_CONFLICT_STATUS=75` and `ANVIL_EMACS_USE_SYSTEM_LOG=1`; the latter opts into `/usr/bin/logger`, preserving startup diagnostics in the bounded macOS system log without an append-only private log file.
- The exact final generation passed the native x86_64 Linux dedicated-smoke derivation `/nix/store/mzfb4klvwqqavhnsgxsfh2cci07r77pv-anvil-mcp-dedicated-smoke.drv`, realized as `/nix/store/fq9hzjj9fbg8xs8j0a5aaaacmrsqf43j-anvil-mcp-dedicated-smoke`, and the native aarch64 Linux derivation `/nix/store/l0q30dcl0jc0bfmva3bp4pk6v5q2xy3b-anvil-mcp-dedicated-smoke.drv`, realized as `/nix/store/qqrilfgwzb32lv8z3km2ag7szjv6w654-anvil-mcp-dedicated-smoke`. Both reported two isolated daemons, 89 unified tools, and 76 typed tools. The same generation is activated and fresh-client-proved on Vulcan, Andoria-08, Andoria-t2, Delphi-3bd4, and gpu-server.
- Focused Home Manager evaluation proves both Boolean defaults and true branches: interactive/no-agent versus dedicated/user-domain launchd on Darwin, and NeLisp/no-service versus dedicated/systemd on Linux. The dedicated Darwin module also evaluates against Home Manager release-25.11, where the launchd `domain` option is absent; its evaluated launchd environment contains exactly `ANVIL_EMACS_LOCK_CONFLICT_STATUS=75` and `ANVIL_EMACS_USE_SYSTEM_LOG=1`.
- NeLisp v0.5.1 contains the Rust `anvil-runtime` crate and checked-in Cargo lock. Its released x86_64 artifact contains Anvil modules byte-identical to anvil.el v1.1.1.
- Upstream's released server silently exposes only `hello` when bootstrap fails. The package patch changes that path to fail closed, and the smoke test detects the distinction.
- The pinned source needed one genuine ARM portability fix: use `std::ffi::c_char` rather than hard-coded `i8` at the FFI C-string boundary.
- Vulcan native ARM64: 63 Rust tests passed; the package smoke reported exactly 42 tools and successful file/shell calls; the runtime closure is Emacs-free.
- Andoria-08 native AMD64: the same 63 tests and 42-tool behavior smoke passed; the runtime closure is Emacs-free.
- Actual consumer override evaluations contain exactly one `anvil-mcp` on Vulcan (`/etc/nixos/flake.nix`), VPS (`/etc/nixos/flake.nix`), and Andoria-08 (`~/.config/home-manager/flake.nix`). All three consumer configurations were activated; the shared Andoria profile also covers Andoria-t2, Delphi-3bd4, and gpu-server through host-local runtime roots.
- Vulcan is ARM64 NixOS with an active lingering user systemd manager and local `/run/user/1000`.
- Andoria-08, Andoria-t2, Delphi-3bd4, and gpu-server are AMD64 Ubuntu hosts. They share the same NFS home/profile and UID 158771033, but each has a host-local `/run/user/158771033`, active user systemd, and lingering enabled.
- At the initial audit no Linux host ran an Emacs daemon. Andoria-08's distro Emacs 27.1 was below anvil.el 1.3.0's Emacs 28.2 minimum, and the other audited hosts had no Emacs. The activated Andoria-family backend therefore uses the Nix `emacs30-nox` closure.
- The shared Andoria flake hard-codes `hostname = "andoria-08"`; the daemon and launcher must derive the real hostname at runtime.
- promptdeploy supports stdio MCP on Claude, Codex, Droid, and OpenCode, including remote Claude through its SSH-stdin merge. Exact-item activation now deploys and verifies the Anvil entries without selecting unrelated secret-bearing items, and the reachable hosts passed activation.
- Upstream anvil.el's complete final pinned suite passes after async isolation and cleanup hardening: 107 test files, 2,704 tests discovered, 2,469 executed and passed, 235 capability-gated skips, and zero failures. The separate readiness, post-dispatch, async-isolation, and ownership regressions cover the original non-yielding reproduction, cancellation, framing and unreadable values, callback exits and mutation, transactional ownership, environment propagation, pool retirement, staged-request custody, partial markers, and child cleanup.

## Current Nix changes

- `packages/anvil-mcp/default.nix`, `agent-supervisor.py`, `agent-supervisor-test.py`, and `Cargo.lock`: platform-dispatched NeLisp, interactive-Darwin, and dedicated-Emacs launchers; vendored NeLisp lock; trimmed evaluator source closure; pinned Anvil/anvil-ide/PyMuPDF runtime; authenticated private roots, nested worker directories, and sockets; a process-wide private umask; Linux daemon-lifetime locks on both runtime and state identities; identity-safe stale-tree and read-only-cache pruning; deterministic sockets; native-JIT suppression; fail-fast bootstrap; isolated root and worker state; and worker-pool shutdown.
- `packages/anvil-mcp/no-placeholder-fallback.patch` and `portable-c-char.patch`: fail-closed NeLisp bootstrap and ARM-compatible FFI.
- `packages/anvil-mcp/smoke.nix` and `smoke.py`: exact 42-tool NeLisp behavior gate.
- `packages/anvil-mcp/headless-smoke.nix` and `headless-smoke.py`: bounded fast functional gate covering concurrent two-daemon isolation, hostile root/nested-directory/socket rejection, dual-lock identity and prune-order sentinels, stale temp/worker removal, private-directory attribute scans, worker lifetimes, post-shutdown restart, exact 89/76 manifests, and real eval/file/Org/semantic/direnv calls; it deliberately excludes the long soak.
- `packages/anvil-mcp/persistent-bridge-soak.nix`, `persistent-bridge-soak.py`, and `persistent-bridge-soak-test.py`: standalone 25-cycle check with named setup/cycle/inventory/cleanup/post-cleanup budgets totaling 8,001 seconds, a 9,602-second TERM deadline, a 90-second KILL grace, and an asserted 9,720-second policy ceiling, plus partial latency summary, partial-frame and cleanup-exit regressions, a 50-second yielding-dispatch proof, post-recovery child isolation, and production-`main` SIGTERM cleanup across construction and finalization.
- `config/anvil.nix`: both Boolean options, selected package installation, deferred systemd service, and a user-domain launchd agent compatible with current and release-25.11 Home Manager option schemas that routes diagnostics to the macOS system logger.
- `config/johnw.nix` and `flake.nix`: module import plus focused packages and platform checks, with package/check evaluation decoupled from the complete Hera Darwin configuration while unrelated development shells continue to use stock nixpkgs.
- This plan and handoff document.

## Historical user-owned state

At implementation start the promptdeploy worktree already contained a user-owned `commands/wiggum.md` modification, which was kept out of the Anvil commits. The live `skills/anvil/SKILL.md` buffer also contained the user's unsaved checkpoint section; the section was retained when that buffer was edited through Anvil and saved, and Emacs later retired its autosave artifact normally. The current promptdeploy checkout is clean and pushed at `c308988401fe9a7087aedfeba38bd59143f4cc7d`.

## Verification ledger

| Gate | State | Evidence or next action |
|---|---|---|
| Upstream compatibility tuple | proved | NeLisp v0.5.1 + anvil.el v1.1.1 source/release comparison |
| NeLisp ARM64 native runtime | passed | Vulcan: 63 tests, exact 42-tool behavior smoke |
| NeLisp AMD64 native runtime | passed | Andoria-08: 63 tests, exact 42-tool behavior smoke |
| Emacs-free NeLisp closures | passed | native recursive closure scans on both architectures |
| ARM FFI portability | passed | `std::ffi::c_char` patch compiled and tested on Vulcan |
| Historical interactive Darwin baseline | passed, not final-service certification | 13 main and 65 typed tools through the separately scoped interactive launcher; production certification is the dedicated 89/76 service above |
| Dedicated fast package/init/services | passed current Darwin, aarch64 Linux, and x86_64 Linux gates | exact native checks prove two-daemon isolation, external async execution, same-root rejection, fail-closed direnv, migration, restart/lock recovery, and final 89/76 transcripts; this bounded gate deliberately excludes the long soak |
| Standalone persistent soak | passed final derivation | 25/25 forced recoveries; dispatch 45.504–46.076s, sibling 1.955–2.930s, restart 0.801–1.347s, readiness 2.252–10.998s; no first-to-last degradation; bounded signal-safe teardown and empty quiet state |
| Read-only orphan cleanup | passed focused and live regression | 87-test suite covers nested mode-0555 cleanup, outside symlink preservation, and pre-open replacement rejection; production pruner removed the known orphan |
| Boolean Home Manager options | passed focused evaluation | both defaults and both true service branches evaluate with the intended backend |
| Shared-home socket isolation | passed runtime smoke | deterministic platform roots, runtime hostname, distinct host sockets and schema/state trees |
| Generic Home Manager outputs | passed | each architecture evaluates to exactly one `anvil-mcp` package with backend `nelisp` |
| Clio Darwin deployment | passed and activated | generation 222, exact final source/hash, preserved dirty checkout, 89/76 live proof including worker reset/root continuity, 2 MiB transport, and empty cleanup |
| Vulcan consumer override | passed and activated | generation 2210, exact current closure, 89/76 live proof including 2 MiB transport and simultaneous-client isolation, pushed lock commit `6a481519` |
| VPS consumer override | intentionally unchanged standalone baseline | actual NixOS/Home Manager graph continues to use its separately pinned 42-tool NeLisp backend, not the `74c33b3` dedicated Emacs generation |
| Andoria-08 consumer override | passed and activated | pushed exact-input commits `c6ee304` and `f1cecc3`; shared Home Manager generation 162 and profile generation 158 |
| Andoria dedicated fleet | passed current work fleet | all 1,931 closure references valid on four hosts; each fresh client passed 89/76 tools, eval, passthrough, worker-pool reset/recovery, and empty cleanup; Andoria additionally passed a real 2 MiB request |
| promptdeploy selection/rendering | current source activated on every dedicated fleet host | exact 330/330-second rendering and fail-closed exact-item activation at `c308988`; Hera, Clio, Vulcan, and the Andoria family use this pin |
| Full repository gates | passed current source tree and complete fleet | aggregate upstream suite, `nix flake check --no-build --all-systems`, exact final 25-cycle soak, both Darwin system builds/switches, native aarch64/x86_64 fast gates, and every fleet fresh-client proof passed |
| Commit audits and partner cleanup | enforced | promptdeploy feature audit clean; initial and hardening-commit Nix audit findings are addressed; the final Nix follow-up receives the required post-commit audit |

## Release-close procedure

1. Keep repository units atomic and stage only explicit task paths.
2. Require an independent fess audit after each logical commit and address every verified finding.
3. Run a separate reproducible regression file for every anvil.el hang fix, then run the bounded dedicated fast check and standalone 25-cycle persistent-soak attribute separately before any live-client soak.
4. Restart complete Codex and Claude OS processes after activation and run the live concurrent soak; do not treat session resume or daemon PID replacement as proof that an old stdio bridge upgraded.
5. Recheck upstream-base currency and final repository state before handoff.

## Stop-and-escalate counts

- Generic Home Manager exact-one-package evaluation: the initial name-only filter exposed a real duplicate caused by the flake's `default` package aliases. Removing those aliases fixed the graph; both architectures now return exactly one backend-bearing package.
- Resolved gate failures: the original 20-second soak-only watchdog falsely killed cold Git under parallel load; the SIGTERM harness initially asserted child-side exit logging after SIGKILL; the timeout-ordering fake omitted the socket-race text required for retry; and the async compatibility probe used an artificial five-second cold-start budget. Production policy alignment, parent-side cleanup evidence, a deterministic transient probe, and a bounded 30-second string-timeout probe resolved them.
- Integrated `host-a` request-10 ambiguous dispatch: two occurrences. The first led to the direct-Python trampoline and removed one cold dyld launch; the second isolated the trigger to the host smoke's artificial six-second dispatch deadline. The final dedicated fast gate, production-policy soak, flake check, and complete system build now pass with the independent accelerated recovery test retained.
- The former local Andoria checkout conflict is not an active blocker: the authoritative remote Home Manager flake was evaluated directly.

## 2026-07-23 root-resilience deployment refresh

This section supersedes conflicting source-pin, timeout, deployment, and verification claims above; earlier sections remain append-only history.

- Signed Nix commit `9826fabb2e3adcc7cf52ed22282c38ec001fdc37` pins anvil.el `39f9c59bfc51379db6243b1be20edca1ea783c2b` with NAR hash `sha256-hh8OrRHTj1ux3OA36b/NUwadseGVk8X/B/U0MFeb5hc=`. Upstream CI run `30009823082` passed all nine jobs.
- The 29-commit review delta `41a0145...39f9c59` adds bounded inline and paginated results, aggregate worker-startup deadlines, correct cold-lazy-worker reporting, bounded stdio runner control, and delayed-runner custody. Upstream PR [#58](https://github.com/zawatton/anvil.el/pull/58) depends on #55; GitHub's displayed diff includes #55 because its base branch exists only on the fork.
- The final policy keeps heartbeat, synchronous evaluation, root watchdog, and bridge dispatch at 45, 120, 225, and 250 seconds. Bounded runner overhead is 19 seconds; inline, large-request, and startup envelopes are 474, 513, and 443 seconds, all below the 540-second client limits. External asynchronous work retains its independent 600-second limit.
- `direnv exec . nix flake check -L --max-jobs 1 --cores 1 path:.` passed at `9826fab`, including formatting, linting, Home Manager smoke, the dedicated Anvil check, and the persistent recovery check. The corrected final-derivation soak passed 25/25 cycles: dispatch median 46.675 seconds and maximum 47.723; readiness median 10.352 and maximum 13.973; restart median 0.987 and maximum 1.221; healthy-sibling median 7.484 and maximum 11.269.
- Hera built target closure `/nix/store/781r3mp1wv2d4y5zldl5wgwmarjy18rj-darwin-system-26.11.57a3171`. Its exact Anvil package `/nix/store/6q7szgc2sir3hyqs51n02k1hz8y0kfv4-anvil-mcp-1.3.0` then passed three waves of four concurrent fresh clients: all twelve roots were distinct, file reads and worker probes succeeded, stderr was empty, and every test-owned process quiesced.
- The Hera system was deliberately **not activated**. Home Manager's separate Nix-managed-agent first-adoption guard stopped on existing generated leaves; an exhaustive dry audit found 574 collisions requiring that project's protected migration and rollback procedure. No bulk migration or overwrite was performed. The temporary Anvil-skill move was restored byte-for-byte, and both `/run/current-system` and the system profile were restored to generation 890 at `/nix/store/nyzzin4m44zqyj43i3daajrrghzc0v42-darwin-system-26.11.57a3171`.
- Nix-config issue [#13](https://github.com/jwiegley/nix-config/issues/13) tracks that protected Hera first-adoption migration separately; it is not an Anvil root-resilience acceptance criterion.
- Nix-config issues #10, #11, and #12 remain intentional nonblocking follow-ups. Upstream issues #57 and #53 also remain open; #53 records PR #58 and the completed Hera target-store proof but awaits both PRs landing and field confirmation.
